name: Setup Flutter App
description: Checkout, setup Flutter and install dependencies

runs:
  using: "composite"
  steps:
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: "3.41.9"
        channel: stable

    - name: Install dependencies
      shell: bash
      run: flutter pub get
